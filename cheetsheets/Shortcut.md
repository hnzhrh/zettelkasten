---
"type:": cheet-sheet
"title:": Shortcut
id:: 20250919102432  # 唯一 ID，基于创建时间确保全局唯一
created:: 2025-09-19T10:24:32  # 创建时间（ISO 格式）
url: 
tags:
  - cheet-sheet
---

# Chrome

`⌘ + L` 地址栏输入
`⌘ + T` 新建 Tab

# Goland

`⌃ + D` Debug

# IDEA

| Key         | Description                             |
| ----------- | --------------------------------------- |
| `⌘ + K`     | Git Commit                              |
| `⌘` + 1     | Show Project In Tool Window             |
| `⌥ + F1`    | Select in. For example, file structure. |
| `⌘ + E`     | Recent Files.                           |
| `⌘ + ⌥ + M` | Extract the selected code as a method.  |
| `⌃ + ⌥ + O` | Optimize imports.                       |
| `⌘ + D`     | Show Git difference.                    |
| `⌘ + W`     | Close the tab.                          |
| `⌘ + ⇧ + U` | Upper case.                             |
| `⌘ + ⬅️`    | Make cursor to line head.               |
